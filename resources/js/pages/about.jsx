import AppLayout from "../layouts/app-layout";

export default function About({ }) {
    return (
        <AppLayout>
            <div>
                <h1>About</h1>
            </div>
        </AppLayout>
    );
}